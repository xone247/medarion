# 💬 General Chat Context

**Copy this to start a new general chat:**

---

I'm working on the **Medarion Healthcare Platform**.

## 📋 Context Files to Read:
- `PROJECT_KNOWLEDGE_BASE.md` - Complete project information
- `DEVELOPMENT_WORKFLOW.md` - General development workflow
- `WORK_FROM_ANYWHERE.md` - Working from anywhere guide
- `QUICK_REFERENCE.md` - Quick commands reference

## 🚀 Current Setup:
- **Repository:** https://github.com/xone247/medarion (should be private)
- **Full Deploy:** `.\qd.ps1 -Deploy` (frontend + backend)
- **Frontend Only:** `.\qfi.ps1`
- **From Git:** `.\deploy_from_git.ps1`

## 🎯 Current Focus:
[Describe what you're working on]

## 📁 Project Structure:
- Frontend: `src/` (React/TypeScript/Vite)
- Backend: `server/` (Node.js/Express)
- PHP API: `api/` directory
- Database: MySQL

## ⚡ Key Commands:
```bash
.\qd.ps1 -Deploy       # Full stack deployment
.\qfi.ps1              # Frontend only
.\deploy_from_git.ps1  # Deploy from Git
git pull               # Get latest
git add -A && git commit -m "msg" && git push  # Commit and push
```

## 🔄 Workflow:
1. Pull latest: `git pull`
2. Make changes
3. Deploy: `.\qd.ps1 -Deploy`
4. Commit: `git add -A && git commit -m "msg" && git push`

## 📝 Notes:
- All code is in GitHub
- Can work from anywhere (Cursor Online, any computer)
- Sensitive files are in .gitignore
- Everything is synced through Git

---

**Help me with:** [Your specific task or question]


